#include <iostream>
#include "Vector.h"
// to include code who's implementation and definition are seperate,
//	include the header

using namespace std;

int main() {
	Vector v(6);
	cout << v[0] << endl;
	
	char wait;
	cin >> wait;
	return 0;
}